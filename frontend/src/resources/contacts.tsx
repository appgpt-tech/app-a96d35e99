//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const contactsTitle = () => {
  const record = useRecordContext();
  return <span>contacts {record ? `"${record.contactName}"` : ''}</span>;
};

export const contactsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <ReferenceField source="contactName" reference="users" />
      <ReferenceField source="profilePictureURL" reference="users" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const contactsEdit = () => (
  <Edit title={<contactsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="contactName" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="profilePictureURL" reference="users" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const contactsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="contactName" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="profilePictureURL" reference="users" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  <ReferenceInput
    source="contactName"
    label="contactName"
    reference="users"
    alwaysOn
  />,
  <ReferenceInput
    source="profilePictureURL"
    label="profilePictureURL"
    reference="users"
    alwaysOn
  />,
];
