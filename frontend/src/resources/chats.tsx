//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const chatsTitle = () => {
  const record = useRecordContext();
  return <span>chats {record ? `"${record.chatName}"` : ''}</span>;
};

export const chatsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <TextField source="chatID" />
      <ReferenceField source="participants" reference="users" />
      <TextField source="chatName" />
      <ReferenceField source="lastMessage" reference="messages" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const chatsEdit = () => (
  <Edit title={<chatsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="chatID" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="participants" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="chatName" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="lastMessage" reference="messages" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const chatsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <TextInput source="chatID" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="participants" reference="users" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="chatName" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="lastMessage" reference="messages" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  <ReferenceInput
    source="participants"
    label="participants"
    reference="users"
    alwaysOn
  />,
  ,
  <ReferenceInput
    source="lastMessage"
    label="lastMessage"
    reference="messages"
    alwaysOn
  />,
];
